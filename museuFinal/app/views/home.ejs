<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <title>Estoque</title>
  <style>
    .center {
      display: flex;
      justify-content: center;
      align-items: center;
      margin-top: 20px;
    }

    .center-title {
      display: flex;
      justify-content: center;
      align-items: center;
      border: 1px solid lightblue;
      margin-top: 20px;
    }
  </style>
</head>

<body>
  <header>
    <%- include('header.ejs'); %>
  </header>
  <div>
    <div class="center-title">
      <h4>Museu Menu</h4>
    </div>
    <div class="container">
      <!-- <div class="center">
        <form action="/ativo/inserir" method="post"><button type="submit">Registrar ativo</button></form>
      </div> -->
      <div class="center">
        <form action="/ativo/atualizar" method="get">
          <input type="number" name="id" placeholder="ID" required>
          <button type="submit" value="enviar">Atualizar ativo</button>
        </form>
      </div>
      <div class="center">
        <form action="/ativo/remover" method="get">
          <input type="number" name="id" placeholder="ID" required>
          <button type="submit" value="enviar">Deletar ativo</button>
        </form>
      </div>
      <div class="center">
        <form action="/ativo/pesquisar" method="get">
          <input type="number" name="id" placeholder="ID" required>
          <button type="submit" value="enviar">Pesquisar ativo</button>
        </form>

      </div>
      <div class="center">
        <form action="/ativo/listar" method="get"><button type="submit">Listar tudo</button></form>
      </div>
    </div>
    <% if (obrasdearte !=0 ) { %>
    <h2 class="center-title">Ativos registrados</h2>
    <% for(let i=0; i<obrasdearte.length; i++) { %>
    <div class="container">
      <div class="row">
        <div class="col-sm">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"> <%= obrasdearte[i].tipo %> <%= obrasdearte[i].marca %></h5>
              <p class="card-text"><%= obrasdearte[i].modelo %>, SN: <%= obrasdearte[i].serial_number %></p>
              <p class="card-text">ID <%= obrasdearte[i].id_ativo %></p>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%}} else {%>
    <div class="card-columns">
      <div class="card">
        <div class="card-body">
          <h4>Banco de dados vazio!</h4>
        </div>
      </div>
    </div>
    <%}%>

  </div>
</body>

</html>