<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="utf-8" />
  <title>Registrar Ativo</title>
</head>

<body>
  <header>
    <%- include('../header.ejs'); %>
  </header>
  <section>
    <% if(errors) { %>
    <ul>
      <% for(let i=0; i < errors.length; i++){ %>
      <li>
        <%= errors[i].msg %>
      </li>
      <% } %>
    </ul>
    <% } %>
  </section>
  <div class="d-flex justify-content-center" style="margin-top: 20px;">
    <h3>Registrar Transferência</h3>
  </div>
  <div class="d-flex justify-content-center">
    <img src="https://cdn-icons-png.flaticon.com/512/179/179971.png" alt="" width="250" height="250">
  </div>
  <div class="d-flex justify-content-center p-1" style="margin-top: 20px;">
    <form action="/transferencia/salvar" method="post">
        <label>Desgaste</label>
        <select name="desgaste" class="form-control">
          <option selected>Selecione</option>
          <option value="Novo">Novo</option>
          <option value="Semi-novo">Semi-novo</option>
          <option value="Usado">Usado</option>
          <option value="Marcas de uso">Marcas de uso</option>
        </select>
        <div class="form-group" style="width: 350px;">
        <label >Serial Number</label>
        <input type="text" class="form-control" name="serial_number" required>
        <label>Responsável atual</label>
        <input type="text" class="form-control" name="marca" required>
        <label>Novo responsável</label>
        <input type="text" class="form-control" name="modelo" required>
        <div class="d-flex justify-content-center" style="margin-top: 20px;">
          <button type="submit" value="enviar" class="btn" style="background-color: lightblue;">Transferir</button>
        </div>
        </div>
    </form>
  </div>
</body>

</html>